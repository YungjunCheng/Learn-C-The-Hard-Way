+ 找到尽可能多的方法使ex3崩溃。

    删除参数：移除printf中的age参数（如printf("I am %d years old.\n");），会导致未定义行为（打印内存垃圾值）
    未初始化变量：将int age = 10;改为int age;，未初始化的局部变量可能输出随机值（如0、负数或垃圾值）
    格式字符串与参数类型不匹配：例如用%s输出int类型（如printf("Age: %s", age);），可能导致段错误
    指针错误：传递空指针或无效指针给%p，如printf("Ptr: %p", NULL);
    栈溢出：在printf中嵌套过多递归格式说明符（如%n的滥用）

+ 执行man 3 printf来阅读其它可用的'%'格式化占位符。如果你在其它语言中使用过它们，应该看着非常熟悉（它们来源于printf）。
核心格式说明符语法：

C
%[flags][width][.precision][length]type

常用类型符（type）：

    %d/%i：有符号十进制整数
    %u：无符号十进制整数
    %o：无符号八进制
    %x/%X：无符号十六进制（小写/大写）
    %f/%F：浮点数（默认6位小数）
    %e/%E：科学计数法（如1.23e+04）
    %g/%G：自动选择%f或%e（去除无效零）
    %c：单个字符
    %s：字符串
    %p：指针地址
    %%：输出%字符

标志（flags）：

    -：左对齐
    +：强制显示符号（正数前加+）
    0：用零填充空白
    #：显示进制前缀（如0x）
    空格：正数前加空格代替+

宽度与精度：

    %5d：输出至少5字符宽度，不足补空格（右对齐）
    %05d：用零填充至5位（如00123）
    %.2f：保留2位小数
    %"*"."*"f：动态指定宽度和精度（如printf("%"*"."*"f", 5, 2, 3.1415);输出 3.14）

转义序列：

    \n：换行
    \t：水平制表符
    \\：反斜杠
    \"：双引号

+ 将ex3添加到你的Makefile的all列表中。到目前为止，可以使用make clean all来构建你所有的练习。
见excise/ex3/Makefile

+ 将ex3添加到你的Makefile的clean列表中。当你需要的时候使用make clean可以删除它。
见excise/ex3/Makefile
